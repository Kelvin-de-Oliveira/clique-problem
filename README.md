# Benchmark de Algoritmos para Clique Máxima

Este projeto permite implementar e testar diferentes algoritmos de clique máxima em grafos, medindo tempo de execução e uso de memória.

## Requisitos

- Python 3.10+
- Dependências listadas em `requirements.txt`

Instale-as com:

```bash
pip install -r requirements.txt
